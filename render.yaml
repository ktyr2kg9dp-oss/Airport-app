# Render Blueprint — deploys Otable as a free always-on web app.
# In Render: New + → Blueprint → pick this repo → Apply. You'll get a public
# URL you can open from any phone. No secrets required.
services:
  - type: web
    name: otable
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
